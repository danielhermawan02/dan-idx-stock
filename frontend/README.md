# Frontend

Next.js frontend for the IDX Stock Screener.

Run from this directory:

```bash
npm install
npm run dev
```

Use `NEXT_PUBLIC_API_URL=http://localhost:8000` for local backend access.

Project-wide setup, verification, deployment, and data-source notes are documented in the root `README.md`.
